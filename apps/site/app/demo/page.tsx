import Link from "next/link";

export default function DemoPage() { return <article className="doc wrap"><p className="eyebrow">Demo status</p><h1>Local proof passed. Cloud handoff is blocked.</h1><p className="intro">The managed Codex interruption, byte-identical checkpoint restore, 51 core checks, six browser checks, authenticated receipt API, and isolated wheel install have passed. The hosted live fixture and recorded replay stay unavailable until Strands/Bedrock recovery and the protected importer suite produce a release receipt.</p><div className="callout"><strong>Current state: provider authorization blocked</strong><p>The authenticated account reports the Nova Micro agreement, entitlement, and region as available, but authorization as <code>NOT_AUTHORIZED</code>. Both tested invocation paths return <code>Operation not allowed</code>. No simulated run is presented as live evidence.</p></div><Link className="secondary" href="/how-it-works">Read how recovery works</Link></article>; }
