export default function JudgesPage() { return <article className="doc wrap"><p className="eyebrow">Agents for Humans / Professional Agents</p><h1>Judge the evidence path.</h1><p className="intro">Dovet preserves approved coding state across a managed worker interruption and independently verifies the recovered result.</p><h2>Reproduce locally</h2><pre><code>pnpm setup{`\n`}pnpm doctor{`\n`}pnpm check{`\n`}pnpm test{`\n`}pnpm test:e2e{`\n`}pnpm release:check</code></pre><p>The isolated install check builds the wheel in a fresh Python 3.12 environment and verifies the CLI, packaged migration, supervisor, and worker imports.</p><h2>Architecture</h2><p>An independent local daemon owns the append-only ledger, workspace leases, checkpoints and process groups. The Codex plugin is a small stdio bridge. A Strands supervisor returns typed advice; local deterministic policy authorizes dispatch. Protected tests run outside worker write scope.</p><h2>Release status</h2><p>Local interruption, checkpoint, API, install, and browser evidence has passed. The exact live provider request IDs, final video, and public source links will appear only after their release gates pass. Missing evidence is displayed as unavailable.</p></article>; }
