import Link from "next/link";
import Image from "next/image";

const steps = [
  ["01", "Save the working state.", "A checkpoint records the approved task, selected files, evidence and last verified result."],
  ["02", "Recover within your rules.", "A Strands supervisor recommends the next move. Policy, budget and workspace ownership determine whether it can proceed."],
  ["03", "Check before calling it done.", "Independent verification runs against the recovered snapshot. Failed or unavailable checks stay visible."],
] as const;

export default function Home() {
  const demoReady = process.env.NEXT_PUBLIC_DEMO_READY === "true";
  const demoLabel = demoReady ? "Run the demo" : "View demo status";
  return <><section className="hero wrap"><p className="eyebrow">Coding work, kept in motion.</p><h1>Keep the work.<br /><span>Change the agent.</span></h1><p className="hero-copy">Dovet saves the useful state of a coding task, hands interrupted work to an authorized worker, and checks the result before calling it finished.</p><div className="actions"><Link className="primary" href="/demo">{demoLabel}</Link><Link className="secondary" href="/docs/install">Install locally</Link></div><p className="qualifier">Codex integration. Strands recovery. Your rules.</p></section><section className="evidence-band" aria-labelledby="evidence-title"><div className="wrap evidence-layout"><div><p className="eyebrow">Build evidence</p><h2 id="evidence-title">The local recovery path is already measurable.</h2><p>The Bedrock handoff remains blocked until the build account authorizes Nova Micro. Local evidence stays separate from that missing cloud proof.</p></div><ol className="rail"><li><time>Passed</time><strong>Managed Codex interruption</strong><span>Owned turn stopped through the supported SDK</span></li><li><time>Passed</time><strong>Immutable checkpoint restore</strong><span>Selected files restored byte for byte</span></li><li><time>51 + 6</time><strong>Core and browser checks</strong><span>Independent local tests plus desktop and mobile UI checks</span></li><li><time>Blocked</time><strong>Live Strands recovery</strong><span>Nova Micro account authorization is not yet available</span></li></ol></div></section><section className="wrap product-proof" aria-labelledby="product-proof-title"><div className="product-proof-copy"><p className="eyebrow">Actual product UI</p><h2 id="product-proof-title">Every green state has a receipt behind it.</h2><p>Dovet accepts only a validated PASS receipt for this view. The release recorder uses the same gate and refuses to capture a simulated cloud success.</p><dl><div><dt>Durability</dt><dd>An independent launchd service survives the Codex bridge exiting.</dd></div><div><dt>Portable recovery</dt><dd>A sanitized bundle verifies every path and hash before restore.</dd></div><div><dt>Install proof</dt><dd>The built wheel passes in a fresh Python 3.12 environment.</dd></div><div><dt>Evidence labels</dt><dd>Observed, reported, inferred and independently verified facts stay distinct.</dd></div></dl></div><figure><Image src="/dovet-evidence-fixture.png" width={1600} height={1000} alt="Dovet local console showing a managed recovery evidence rail and independent verification receipt" priority /><figcaption>Deterministic contract fixture for UI verification. Live Bedrock evidence is still blocked.</figcaption></figure></section><section className="wrap prose-split"><div><p className="eyebrow">The problem</p><h2>The interruption is only the first problem.</h2></div><div><p>After a coding agent stops, you still have to work out what changed, what passed, what failed, and what the next worker needs to know. Dovet keeps that recovery work attached to the task.</p><ul className="row-list"><li>Task brief</li><li>Scoped changes</li><li>Verified checks</li><li>Next action</li></ul></div></section><section className="wrap process"><p className="eyebrow">How it works</p><h2>Recovery with an audit trail.</h2><ol>{steps.map(([number,title,copy]) => <li key={number}><span>{number}</span><h3>{title}</h3><p>{copy}</p></li>)}</ol><p className="limitation">Automatic recovery applies to Dovet-managed runs. Existing desktop sessions can be observed and handed off with your involvement.</p></section><section className="oxide"><div className="wrap boundary"><div><p className="eyebrow">Boundaries</p><h2>Autonomy with an edge you can see.</h2><p>Dovet does not rotate ChatGPT accounts, merge code for you, or let a worker approve its own permissions. It pauses when the next action needs a decision.</p></div><dl><div><dt>Approved paths</dt><dd>The worker sees only the configured project scope.</dd></div><div><dt>Authorized workers</dt><dd>Recovery choices come from an explicit provider list.</dd></div><div><dt>Explicit budget</dt><dd>Policy denies dispatch without an approved bound.</dd></div></dl></div></section><section className="wrap local"><p className="eyebrow">Local-first</p><h2>Your workspace stays yours.</h2><p>Project state and checkpoint history are stored locally by default. Your approved coding providers receive the context needed to do the task. Cloud evidence sharing is explicit, and the public demo uses synthetic code.</p><Link href="/docs/security">Read the data boundaries</Link></section><section className="wrap final-cta"><h2>Let the next worker start with evidence.</h2><div className="actions"><Link className="primary" href="/demo">{demoLabel}</Link><Link className="secondary" href="/docs/install">Read installation guide</Link></div></section></>;
}
