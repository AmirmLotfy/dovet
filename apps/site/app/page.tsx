import Link from "next/link";

const steps = [
  ["01", "Save the working state.", "A checkpoint records the approved task, selected files, evidence and last verified result."],
  ["02", "Recover within your rules.", "A Strands supervisor recommends the next move. Policy, budget and workspace ownership determine whether it can proceed."],
  ["03", "Check before calling it done.", "Independent verification runs against the recovered snapshot. Failed or unavailable checks stay visible."],
] as const;

export default function Home() {
  return <><section className="hero wrap"><p className="eyebrow">Coding work, kept in motion.</p><h1>Keep the work.<br /><span>Change the agent.</span></h1><p className="hero-copy">Dovet saves the useful state of a coding task, hands interrupted work to an authorized worker, and checks the result before calling it finished.</p><div className="actions"><Link className="primary" href="/demo">Run the demo</Link><Link className="secondary" href="/docs/install">Install locally</Link></div><p className="qualifier">Codex integration. Strands recovery. Your rules.</p></section><section className="evidence-band" aria-labelledby="evidence-title"><div className="wrap evidence-layout"><div><p className="eyebrow">Recorded evidence</p><h2 id="evidence-title">One interruption.<br />A trace you can inspect.</h2><p>The released run will appear here only after its checkpoint and independent checks are recorded.</p></div><ol className="rail"><li><time>Pending</time><strong>Managed worker interruption</strong><span>Waiting for released run evidence</span></li><li><time>Pending</time><strong>Checkpoint validation</strong><span>No result published yet</span></li><li><time>Pending</time><strong>Independent verification</strong><span>Unknown until checks run</span></li></ol></div></section><section className="wrap prose-split"><div><p className="eyebrow">The problem</p><h2>The interruption is only the first problem.</h2></div><div><p>After a coding agent stops, you still have to work out what changed, what passed, what failed, and what the next worker needs to know. Dovet keeps that recovery work attached to the task.</p><ul className="row-list"><li>Task brief</li><li>Scoped changes</li><li>Verified checks</li><li>Next action</li></ul></div></section><section className="wrap process"><p className="eyebrow">How it works</p><h2>Recovery with an audit trail.</h2><ol>{steps.map(([number,title,copy]) => <li key={number}><span>{number}</span><h3>{title}</h3><p>{copy}</p></li>)}</ol><p className="limitation">Automatic recovery applies to Dovet-managed runs. Existing desktop sessions can be observed and handed off with your involvement.</p></section><section className="oxide"><div className="wrap boundary"><div><p className="eyebrow">Boundaries</p><h2>Autonomy with an edge you can see.</h2><p>Dovet does not rotate ChatGPT accounts, merge code for you, or let a worker approve its own permissions. It pauses when the next action needs a decision.</p></div><dl><div><dt>Approved paths</dt><dd>The worker sees only the configured project scope.</dd></div><div><dt>Authorized workers</dt><dd>Recovery choices come from an explicit provider list.</dd></div><div><dt>Explicit budget</dt><dd>Policy denies dispatch without an approved bound.</dd></div></dl></div></section><section className="wrap local"><p className="eyebrow">Local-first</p><h2>Your workspace stays yours.</h2><p>Project state and checkpoint history are stored locally by default. Your approved coding providers receive the context needed to do the task. Cloud evidence sharing is explicit, and the public demo uses synthetic code.</p><Link href="/docs/security">Read the data boundaries</Link></section><section className="wrap final-cta"><h2>Let the next worker start with evidence.</h2><div className="actions"><Link className="primary" href="/demo">Run the demo</Link><Link className="secondary" href="/docs/install">Read installation guide</Link></div></section></>;
}
