export default function SecurityPage() { return <article className="doc wrap"><p className="eyebrow">Docs / Security</p><h1>Data and control boundaries.</h1><h2>Local state</h2><p>The SQLite event ledger, policies and content-addressed checkpoints stay in the local Dovet data directory by default with owner-only permissions.</p><h2>Provider data</h2><p>Codex and Bedrock receive only context permitted by the selected project profile. Credentials remain in their provider tooling; Dovet does not copy session cookies or rotate identities.</p><h2>Process ownership</h2><p>Automatic interruption applies only to managed process groups Dovet started and can identify by PID and birth time. Lease expiry alone never proves a worker stopped.</p><h2>Known limits</h2><p>Dovet cannot reconstruct hidden reasoning, unsaved bytes, or continue local execution while the machine sleeps. Passing configured checks does not prove behavior those checks did not cover.</p></article>; }
