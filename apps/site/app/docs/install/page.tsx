export default function InstallPage() { return <article className="doc wrap"><p className="eyebrow">Docs / Install</p><h1>Install locally.</h1><p className="intro">The current build is source-installable on macOS and is not notarized.</p><h2>Prerequisites</h2><p>Python 3.12, uv, Node 24, pnpm 11, Git and an installed authenticated Codex application.</p><pre><code>pnpm setup{`\n`}pnpm doctor{`\n`}pnpm test{`\n`}dovet init-state{`\n`}dovet serve</code></pre><h2>Background service</h2><p><code>dovet service install</code> creates an opt-in per-user launchd service. The local ledger and checkpoints remain after uninstall so recovery evidence is not silently deleted.</p><h2>Cloud recovery</h2><p>Bedrock is disabled until AWS credentials, a model invocation probe and an explicit budget are configured. Local checkpoint tests do not claim cloud readiness.</p></article>; }
