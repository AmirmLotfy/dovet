export default function CheckpointsPage() { return <article className="doc wrap"><p className="eyebrow">Docs / Checkpoints</p><h1>Files bound to evidence.</h1><p className="intro">Each immutable manifest names an approved source revision, selected relative paths and SHA-256 content digests.</p><h2>Capture</h2><p>Files are read only after the managed writer stops. Dovet compares file identity, size and timestamps before and after reading to reject unstable capture.</p><h2>Restore</h2><p>Imports validate canonical hashes, path containment, Unicode and case collisions, symlinks and referenced blobs before materializing a separate candidate directory.</p><h2>Completeness</h2><p>A missing or corrupt blob makes a checkpoint ineligible for automatic recovery. Unknown evidence remains unknown.</p></article>; }
