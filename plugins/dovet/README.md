# Dovet Codex plugin

This plugin is the user-facing bridge to an independently installed Dovet local service. It does
not keep work alive by itself and it does not start background monitoring on installation.

From the repository root, install Python dependencies and start the service before using the MCP
tools. Removing the plugin leaves the local checkpoint ledger in place.
